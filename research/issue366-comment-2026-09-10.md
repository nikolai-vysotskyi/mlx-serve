@ddalcu @beamivalice — cloud-agent checkpoint (branch `arena/01a08be3-mlx-serve`, HEAD `fe2de89`). Three opt-in prefill kernels implemented and CPU-validated; **none built on Metal yet, no tok/s claim.**

1. **Chunkwise GDN** — rank-1 fold / boundary scan / parallel replay replacing the per-token sequential state recurrence. Env `MLX_SERVE_GDN_CHUNKED=1` (off by default). CPU evidence: `research/gdn_chunked_reference.py` (f64 golden; f32 chunk error == stock) and `research/gdn_chunked_kernel_sim.py` (index-exact sim vs f64-validated chunk_scan, PASS at T=128/500/1024/2050). Two index bugs already found & fixed by review+sim (fold stored A_c transposed; fold grid under-counted by 256×).

2. **Fused HC up-projection + 4-stream mix** — folds `sigmoid(up)·n4` and the mean into the up-projection GEMM epilogue, so the `[B,S,hc*H]` (~168 MB bf16 at S=8192) intermediate is never materialized. Env `MLX_SERVE_HC_UP_MIX=1`. `research/hc_up_mix_reference.py` PASS: kernel shares every rounding point with the composed chain; sigmoid LUT bit-exact (the repo's swigluSigTable).

3. **Fused MoE down + score + top-K reduce** — the same idea as the 0.674 ms / bit-identical component measured earlier, re-implemented self-contained on current main (gather form, no atomics, coalesced reads). Env `MLX_SERVE_MOE_DOWN_REDUCE=1`. `research/moe_down_reduce_reference.py` PASS vs f64; matches the composed chain's rounding points (bf16 product, fp32 sum via MLX's bfloat16-widened Reduce, bf16 out).

Validation status: all three are whole-file `zig ast-check` clean and pass isolated semantic type-checks against a signature-exact MLX stub; the Metal kernel bodies are **unbuilt** (no Mac here). Request: a maintainer-side `zig build test` plus the three parity tests (`gdnChunkedParityCase`, "fused HC prefill up+mix…", "fused MoE down+score+reduce…") on an M5-class runner, and — if green — an llmprobe A/B at chunk8192 / ctx131072 / prefix-cache 0 / MTP off against current main. Exact commands and the honest per-lever status are in `NEXT.md` at the branch head.
